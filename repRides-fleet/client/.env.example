# Copy to .env.local for local overrides (gitignored).
# Usually unset locally — Vite's dev proxy handles /api → localhost:3001.

# Required in production (Netlify dashboard): point to your Render API URL.
# Example: VITE_API_URL=https://reprides-fleet-api.onrender.com
VITE_API_URL=
